import Clock from "./Clock";
import Calendar from "./Calendar";

const Calendar_Time = () => {
  return (
    <div>
      <Clock />
      <Calendar />
    </div>
  );
};

export default Calendar_Time;
